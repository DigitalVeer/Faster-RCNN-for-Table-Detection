# Runtime dependencies for the modernized Faster R-CNN table detector.
# Target: Python 3.9-3.11.
#
# The `frcnn` library package (config/utils/roi/data/eval) and the test suite
# run on NumPy alone (+ OpenCV / pandas / scikit-learn for the full data and
# evaluation pipeline). TensorFlow is required to build and train the model
# itself; the notebooks are migrated from the legacy TF1.15 / standalone-Keras
# 2.3 stack to tf.keras as a follow-up step.

numpy>=1.23,<2.0
pandas>=2.0
opencv-python-headless>=4.5
scikit-learn>=1.1
matplotlib>=3.5
h5py>=3.6
tensorflow>=2.15,<2.17

# Dev / test
pytest>=7.0
